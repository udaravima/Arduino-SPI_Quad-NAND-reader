#pragma once

// api.h - public API surface for higher-level commands.
// Intentionally minimal: no heavy includes to avoid circular dependencies.

// Add public function declarations here when needed, for example:
// void someApiFunction();

